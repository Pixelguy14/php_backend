Similar to npm install, for php composer we have:
-> $ composer install
To install all dependencies used in this environment

To run the code:
-> $ php -S localhost:8089
And then navigate to the <b>/public</b> url